defmodule AocTest do
  use ExUnit.Case
  doctest Aoc

  test "Pods" do
    # text = ""
    assert Pods.done?({["A","A"],["B","B"],["C","C"],["D","D"]})
  end

  test "part1" do
    # text = ""
    # t = Aoc.parse(text)
    # Aoc.part1(t)
    assert true
  end

  test "part2" do
    # text = ""
    # t = Aoc.parse(text)
    # Aoc.part2(t)
    assert true
  end
end
